<?xml version="1.0" encoding="utf-8"?>
<layout>
    <data>
        <variable
            name="todo"
            type="com.shinobi.todo.entity.ToDo"/>
        <import
            type="com.shinobi.todo.entity.ToDo.Status"/>
    </data>
    <android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_margin="8dp"
        >

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="16dp"
            >

            <TextView
                android:id="@+id/text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="vqnsdmslnmqkn"
                android:textSize="18sp"
                android:text="@{todo.text}"
                android:textColor="@{todo.status == Status.SENT ? @android:color/holo_green_dark : @android:color/black}"
                />

        </LinearLayout>

    </android.support.v7.widget.CardView>
</layout>